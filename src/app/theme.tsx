import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#86A8E4",
    },
    secondary: {
      main: "#C8743C",
      dark: "#92202A"
    },
    text: {
      primary: "#E1E8F4"
    },
    background: {
      default: "#050A0A"
    }
  }
});

export default theme;